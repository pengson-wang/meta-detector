{
  "name": "meta-detector",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm build:core && pnpm build:background",
    "build:core": "vue-tsc --noEmit && vite build",
    "build:background": "vue-tsc --noEmit && vite build --config vite.config_background.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "sass": "^1.57.1",
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.0",
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.18",
    "tailwindcss": "^3.1.8",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vue-tsc": "^0.40.4"
  }
}